default_dirs:
  skills:
    - default: .claude/skills/
    - global: ~/.claude/skills/
  agents:
    - default: .claude/agents/
    - global: ~/.claude/agents/
  prompts:
    - default: .claude/commands/
    - global: ~/.claude/commands/

library:
  skills:
    - name: prompt-enhancer
      description: Improve, rewrite, polish, tighten, or fix a draft prompt the user has already written. Returns only the enhanced prompt with no commentary — paste-ready replacement for the original draft.
      source: C:/Users/George/.claude/skills/prompt-enhancer/SKILL.md
  agents: []
  prompts: []
